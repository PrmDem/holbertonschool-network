# Network
Continuation of our foray into the basics of networking.<br/>

## Requirements
* Allowed editors: vi, vim, emacs
* All files will be interpreted on Ubuntu 22.04
* All files should start with `#!/usr/bin/env bash` and end with a new line
* The second line should be a comment explaining what is the script doing
* All files must be executable
* All scripts must pass shellcheck without any error

## General information
__Number of tasks:__ 3<br/>
__Completed:__ 3<br/>
__Passed:__ 3<br/>

## Tasks overview
#### 0. Change your home IP
Write a Bash script that configures an Ubuntu server where `localhost` resolves to `127.0.0.2` and `facebook.com` resolves to `8.8.8.8`.<br/>
See file [0-change_your_home_IP](./0-change_your_home_IP)

#### 1. Show attached IPs
Write a Bash script that displays all active IPv4 IPs on the machine it’s executed on.<br/>
See file [1-show_attached_IPs](./1-show_attached_IPs)

#### 2. Port listening on localhost
Write a Bash script that listens on `port 98` on `localhost`.<br/>
See file [2-port_listening_on_localhost](./2-port_listening_on_localhost)